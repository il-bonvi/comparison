race replay traslazione (prompt)

osserva molto bene il codice: tab race replay: se osservo corse lunghe, potrei avere corridore che partono a distanze leggermente diverse (in fondo al gruppo). Allineando start e end, ho una visione abbastanza corretta, ma il gap sarà sempre fisso a un valore diverso da zero. Ho bisogno di uno strumento per poter fare dei microspostamenti di "tutta la traccia" a livello temporale. (es. ho il gap che per 2h sta a -7, è scorretto, vorrei vedere 0... quindi vorrei una funzione che mi ritarda un traccia di un valore che decido per avere una maggior precisione). voglio confrontare lo stesso identico percorso, ma c'è un motivo per cui avevo inserito l'allineamento geografico... c'è bisogno di esso e NON va tolto. Il problema è questo: se ho 2 atleti che partono con 15m di distanza, avranno un certo distacco in secondi. Allinenado lo start (cosa che voglio che avvenga per diversi motivi) si allinea la partenza ma avrò un gap in secondi che dovrà essere azzerato, altrimenti, avrò per tutta la durata del percorso un tot di secondi di gap anche se gli atleti hanno fatto la corsa da affiancati. C'è bisogno di una soluzione semplice MA ropbusta a questo problema.


comparison: traslazione di traccia in diversi punti, per uniformare i settori
!!!!NON SHIFTA
Uncaught ReferenceError: _compShiftTimer is not defined
    compSectorShift http://localhost:8002/:1896
    onclick http://localhost:8002/:1

caricamento file: cambiando file il file vecchio da errore
comparison: aggiusta la distanza....
comparison barr laterale: button tabella
dividi comparison per sezioni
comparison: evidenziazione un po' buggato (settori)
aggiustare gli hover

tabella: manca pendenza A-b
colori settori buggati in grafico comparison

comparison ottimizzazione

vari bug fix comparison dei settori
In file molto lunghi in comparison la distance è sballatina

uniformare grafici e topbar
