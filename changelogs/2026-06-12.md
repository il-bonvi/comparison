##### *12/06/2026*

#### Race Replay Sector Sidebar & Sector Zoom

#### ✨ Nuove funzionalità

##### Settori sincronizzati tra Comparison e Race Replay

- Aggiunta la funzione `rpSyncSectorList()`.
- La sidebar dei settori nel Race Replay viene ora popolata automaticamente usando `compSectors`.
- I settori definiti nella tab Comparison vengono mostrati in modalità **read-only** nel Replay.
- Aggiornamento automatico della sidebar quando:
  - vengono creati/modificati settori;
  - viene ricostruita la lista settori (`rebuildSectorList()`).

##### Nuova sidebar "Settori" nel Replay

- Introdotto un pannello laterale dedicato (`#rp-sector-sidebar`).
- Layout coerente con la sidebar della Comparison.
- Visualizzazione di:
  - colore settore;
  - nome settore;
  - range start/end tramite `compSectorRangeLabel()`.

##### Zoom rapido sui settori

Per ogni settore sono disponibili due nuove azioni:

- 🔍 **Reale**
  - Mostra il settore mantenendo il gap temporale reale accumulato.
  - Conserva `rpTimeOffsetB`:
- 🔍 **Allineato**
  - Mostra il settore con partenza sincronizzata.
  - Azzera temporaneamente `rpTimeOffsetB`.

##### Toggle intelligente

- Cliccando nuovamente sul pulsante attivo:
  - viene annullato lo zoom;
  - si ritorna automaticamente alla traccia completa.

##### Ripristino traccia completa

- Nuovo pulsante:
  - `↺ Traccia completa`
- Visibile solo quando è attivo uno zoom settore.
- Ripristina:
  - bounds originali;
  - offset temporale globale;
  - visualizzazione completa della traccia.

***

#### ⚙️ Infrastruttura Bounds

##### Nuovo contesto Replay per bounds.js

Aggiunta:

```text
rpSetupBoundsContext()
```

che registra:

```text
boundsSetContext(() => ({
  sA: rpSA,
  sB: rpSB,
  map: rpMap,
  mapReady: rpMapReady
}));
```

In questo modo il sistema shared di gestione bounds può lavorare anche nel Race Replay.

##### Callback applicazione bounds

Registrato:

```text
boundsSetOnApply(...)
```

che esegue automaticamente:

- `rpBuildAlignment()`
- `rpRenderCharts()`
- `rpUpdateFrame(0)`
- `rpUpdateBoundsMarkersOnMap()`

quando i bounds vengono modificati.

***

#### 🧠 Gestione stato zoom settore

Nuove variabili globali:

```text
let rpZoomedSectorId = null;
let rpSectorAlignMode = null;
let rpSavedTimeOffsetB = 0;
let rpFullBounds = null;
```

Utilizzate per:

- memorizzare il settore attivo;
- distinguere modalità Real/Aligned;
- salvare l'offset temporale globale;
- salvare i bounds originali della traccia.

***

#### 🎨 Miglioramenti UI

##### Nuove classi CSS

```text
.rp-sec-item
.rp-sec-dot
.rp-sec-label
.rp-sec-zoom-real.active
.rp-sec-zoom-aligned.active
```

Aggiunti stati visivi per:

- settore selezionato;
- modalità zoom attiva;
- evidenziazione del settore zoomato.

##### Highlight settore attivo

Il settore zoomato viene evidenziato con:

- sfondo blu trasparente;
- bordo sinistro colorato;
- pulsante attivo evidenziato.

Gestito da:

```text
rpUpdateZoomButtonsUI()
```

***

#### 🔄 Lifecycle

Aggiunta inizializzazione:

```text
rpSetupBoundsContext();
```

nei flussi di apertura del Replay prima di:

```text
rpBuildAlignment();
rpInitChart();
rpRenderCharts();
```

garantendo che il sistema bounds sia operativo fin dall'avvio.

***

### Risultato finale

Il Race Replay ora dispone di una vera navigazione per settori:

- sincronizzata con la Comparison;
- zoom immediato sui singoli settori;
- doppia modalità con gap **Reale / Allineato**;
- ritorno rapido alla traccia completa;
- integrazione completa con il sistema bounds e con mappe/grafici del Replay.
