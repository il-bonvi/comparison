##### 20/06/2026

# Miglioramento controlli UI, pannelli smoothing e layout Comparison/Replay

## 🔄 Modifiche (Changed)

- Introdotto uno stato visuale condiviso `.active` per i pulsanti `btn-ghost`, utilizzato per evidenziare pannelli e strumenti attualmente aperti.
- Uniformato il comportamento dei pulsanti:
  - `Gestisci Settori`
  - `avg` (smoothing)
  - `📊 Tabella`
  - `🎯 Regola Start/End`
  mostrando uno stato attivo coerente all'apertura dei rispettivi pannelli.
- Rimosso l'override `background:var(--border)` dal pulsante **Gestisci Settori**, che impediva la corretta visualizzazione dello stato attivo.
- Ridimensionati alcuni controlli della toolbar (`Gestisci Settori`, `Tabella`, `avg`) con padding e font-size più compatti.

## 🎨 Miglioramenti UI

- Convertito il pannello di smoothing (`comp-smooth-panel` e `rp-smooth-panel`) da elemento inline del layout a dropdown flottante posizionato dinamicamente rispetto al pulsante `avg`.
- Il pannello smoothing viene ora visualizzato tramite posizionamento assoluto/fixed, evitando:
  - spostamenti del layout;
  - reflow della toolbar;
  - effetti di flash durante apertura e chiusura.
- Abilitato `overflow:visible` nelle toolbar di Comparison e Replay per consentire la corretta visualizzazione dei dropdown flottanti.
- Aggiornato lo stile dei pannelli smoothing con layering (`z-index`) e shadow dedicate per migliorarne la leggibilità sopra gli altri componenti.
- Riposizionati i controlli mappa di Comparison (`#comp-map-controls`) nella parte inferiore della mappa.

## 🧠 Logica e stato

- Aggiornata `compToggleSectorPanel()` per sincronizzare automaticamente la classe `active` del pulsante associato all'apertura e chiusura del pannello settori.
- Aggiornata `compToggleSmoothPanel()`:
  - calcolo dinamico della posizione tramite `getBoundingClientRect()`;
  - apertura del pannello in modalità floating;
  - gestione dello stato `active` del pulsante.
- Aggiornata `rpToggleSmoothPanel()` con la stessa logica di posizionamento e gestione stato adottata nella Comparison View.
- Sostituita la precedente logica basata sulla classe `off` per:
  - `comp-btn-table`
  - `comp-btn-bounds`
  - `rp-btn-bounds`

  con l'utilizzo della classe `active`, rendendo il comportamento coerente con gli altri controlli.

## 🎨 Layout Comparison

- Riorganizzata la struttura della Comparison View:
  - `#comp-main` passa da layout orizzontale a layout verticale.
  - Introdotto il nuovo contenitore `#comp-main-row`.
- Separato il pannello grafico dalla sezione mappa/sidebar.
- Il grafico (`#comp-chart-panel`) viene ora renderizzato come area full-width sotto mappa e sidebar, occupando l'intera larghezza disponibile.
- Corretta la struttura HTML dei contenitori principali con riallineamento dei wrapper e dei relativi tag di chiusura.

### Risultato finale

L'interfaccia di Comparison e Replay risulta più stabile e coerente: i controlli mostrano chiaramente il proprio stato attivo, il pannello di smoothing si apre come menu flottante senza alterare il layout e il grafico della Comparison dispone ora di un'area dedicata a larghezza completa sotto mappa e sidebar.